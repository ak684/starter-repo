# OpenHands Starter Repo

A template for [OpenHands](https://app.openhands.ai) that ensures agents preserve work via git branches.

## Usage

1. Create a repo from this template.
2. Start a conversation on OpenHands and select the repo.
3. The agent follows `AGENTS.md` to branch, commit, and push all work.
